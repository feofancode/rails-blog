<%= form_with(model: admin_article, local: true) do |form| %>
  <% if admin_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_article.errors.count, "error") %> prohibited this admin_article from being saved:</h2>

      <ul>
      <% admin_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :admin_article_title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :admin_article_author %>
  </div>

  <div class="field">
    <%= form.label :markdown %>
    <%= form.text_area :markdown, id: :admin_article_markdown %>
  </div>

  <div class="field">
    <%= form.label :html %>
    <%= form.text_area :html, id: :admin_article_html %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
