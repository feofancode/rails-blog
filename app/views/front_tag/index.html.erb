<% provide(:title, "Tags" )%>

<div>
	<% @tags.each do |tag| %>
		<%= link_to "#{tag.name}", "#tag-#{tag.name}" %>
	<% end %>
</div>

<div>
	<%= render partial: "front_tag/tag", collection: @tags %>
</div>
